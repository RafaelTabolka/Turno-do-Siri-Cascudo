export interface ILoginRequest {
    name: string,
    password: string;
}
// Isso é ilustrativo. Como não possuímos um back-end para pegar essas informações e retornar um token para nós, vou deixar esta interface criada para ilustrar como seria o fluxo real de uma aplicação